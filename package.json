{
  "name": "tokenist-core",
  "version": "0.1.0",
  "description": "OpenAI Realtime API proxy with per-user token/cost tracking — Cloudflare Workers edition",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev src/worker.ts --port 8081",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "example:log-with-fetch": "tsx examples/log-with-fetch.ts"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.0",
    "js-tiktoken": "^1.0.15",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.91.0"
  },
  "engines": {
    "node": ">=18"
  }
}
